#include <iostream>
#include <vector>

using namespace std;
#define x first
#define y second

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int n;
    cin >> n;
    vector<pair<pair<int, int>, pair<int, int>>> gg;
    for (int i = 0; i < n; i++){

    }
}