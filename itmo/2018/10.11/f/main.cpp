#include <bits/stdc++.h>
using namespace std;
int main() {
    int64_t v1, v2, l1, l2;
    cin >> v1 >> v2 >> l1 >> l2;
    int64_t a1 = l1 * v1 + l2 * v2;
    int64_t b1 = l1 * v1;
    int64_t c1 = v2 * (l1 + l2);
    int64_t a2 = l1 * v1 + l2 * v2;
    int64_t b2 = -l1 * v2;
    int64_t c2 = v2 * (l1 + l2);
    int64_t ans = 0;
    while (1){
        if (a)
    }
    return 0;
}